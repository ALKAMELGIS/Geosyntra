node_modules/
dist/
frontend/dist/
backend/dist/
*.log

.env
.env.*
!.env.example

# Server-side API token store (Mapbox, Gemini, …) — never commit.
backend/server/agri_api_secrets.json

npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

.DS_Store
Thumbs.db

coverage/
playwright-report/
test-results/

.vite/
.cache/
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

_restore_tmp_*/
# Static GitHub Pages build output (may embed env secrets; regenerate in CI with secrets).
AgroCloud-Pages-Upload/
# Duplicate export tree / archives (use prepare-for-github.ps1 locally; do not commit).
AgroCloud-GitHub-Ready/
AgroCloud-GitHub-Ready.zip
AgroCloud-Pages-Upload.zip

backup_*.zip
*.tmp
*.bak
coverage-final.json
.nyc_output/

# GitHub Pages SPA at repo root (CI commits with git add -f; never commit from a dev machine — .env would embed secrets and push protection blocks).
/assets/
/avatars/
/index.html
/404.html
/favicon.svg
/favicon.png
/favicon-16x16.png
/favicon-32x32.png

# Built SPA copy for manual zip / branch-at-/docs experiments (do not commit: push protection flags embedded tokens).
/docs/
