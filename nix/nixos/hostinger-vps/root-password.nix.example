# Copy to ../../secrets/vps-root-password.nix (gitignored) or let install-nixos-hostinger.sh generate it.
# Console/KVM login only — SSH remains key-only (see networking.nix).
{ users.users.root.password = "change-me"; }
