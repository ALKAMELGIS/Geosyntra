export const appConfig = {
  appName: 'Geosyntra',
  repoName: 'Geosyntra',
  basePath: '/Geosyntra/',
  /** Canonical GitHub Pages URL (no hash). Hash routes use `/#/…` in the browser. */
  productionPublicUrl: 'https://alkamelgis.github.io/Geosyntra/',
} as const
