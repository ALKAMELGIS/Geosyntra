/**
 * Product copy for the in-map **Agent Chat** (formerly surfaced as “Geo AI Chat”).
 * Keep strings here so Satellite + GIS Map stay aligned.
 */

export const AGENT_CHAT_PANEL_TITLE = 'Agent Chat'

/** Gemini / map-explorer welcome (also used for clipboard copy). */
export const AGENT_CHAT_WELCOME_EXPLORER =
  "Hello! I'm the Geosyntra spatial agent — describe a place, upload an image, or ask for directions.\nWhen a location is clear, the map will fly there."

/** Claude / DeepSeek data-assistant tab intro. */
export const AGENT_CHAT_WELCOME_DATA_ASSISTANT =
  'Ask about GIS layers using natural language. The app runs **Select by attributes**, **SQL WHERE**, and **Select by location** (within / intersect) on loaded vectors first — results appear as **interactive tables** (sort, filter, multi-select, export) and sync to the map.'
