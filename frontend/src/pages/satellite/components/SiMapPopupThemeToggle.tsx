type SiMapPopupThemeToggleProps = {
  className?: string;
};

/** Theme toggle removed — popups use a single unified light-gray chrome. */
export function SiMapPopupThemeToggle(_props: SiMapPopupThemeToggleProps) {
  return null;
}
