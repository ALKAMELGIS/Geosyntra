/**
 * Crop health map display uses Mapbox raster + GIS severity layers (see SatelliteIntelligenceMain).
 * This component is kept as a mount point for future canvas-free overlays only.
 */
export type SiMapCropHealthOverlayProps = {
  active: boolean;
};

export function SiMapCropHealthOverlay(_props: SiMapCropHealthOverlayProps) {
  return null;
}
