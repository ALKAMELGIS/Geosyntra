import './AgroCloudDashboard.css'

/**
 * Dashboard page always renders the persistent host container.
 * The embedded iframe is mounted globally by PersistentAgroCloudEmbed for instant route open.
 */
export default function AgroCloudDashboard() {
  return <div className="agro-cloud-route-fill" />
}
