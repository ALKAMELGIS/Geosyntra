/**
 * Geosyntra local dev ports (Geo-Intelligence repo only).
 * AgroCloud (`AgroCloud-main`) uses 5174 / 3011 / 3012 — never reuse here.
 */
export const VITE_PORT = 5173
export const API_PORT = 3001
export const WS_PORT = 3002
